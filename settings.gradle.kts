include(":nullable")
include(":lateinit")
include(":tailrec")
rootProject.name = "kotlin"

pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}