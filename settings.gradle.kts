include(":nullable")
include(":lateinit")
rootProject.name = "kotlin"

pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}