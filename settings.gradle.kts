include(":nullable")
include(":lateinit")
include(":tailrec")
include(":generics")
include(":inline")
include(":function")
include(":callable-reference-operator")
rootProject.name = "kotlin"

pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}