include(":nullable")
include(":lateinit")
include(":tailrec")
include(":generics")
include(":inline")
rootProject.name = "kotlin"

pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}