include(":nullable")
include(":lateinit")
include(":tailrec")
include(":generics")
rootProject.name = "kotlin"

pluginManagement {
    repositories {
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}